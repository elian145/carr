# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-change-in-production
JWT_SECRET_KEY=your-jwt-secret-key-here-change-in-production

# Database Configuration
# For SQLite (default)
# DATABASE_URL=sqlite:///car_listings.db

# For PostgreSQL (uncomment and configure)
USE_POSTGRES=true
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=car_listings
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Or use a full PostgreSQL URL
# DATABASE_URL=postgresql+psycopg2://username:password@localhost:5432/car_listings

# SMS Configuration (for password reset - optional)
# SMS_PROVIDER=twilio
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token
# TWILIO_PHONE_NUMBER=your-twilio-phone-number

# Redis Configuration (optional, for caching and sessions)
REDIS_URL=redis://localhost:6379/0

# Firebase Configuration (for push notifications)
FIREBASE_SERVER_KEY=your-firebase-server-key
FIREBASE_PROJECT_ID=your-firebase-project-id

# AWS Configuration (for file storage)
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_BUCKET_NAME=your-s3-bucket-name
AWS_REGION=us-east-1

# Security Configuration
BCRYPT_LOG_ROUNDS=12
PASSWORD_RESET_EXPIRY=3600

# File Upload Configuration
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=104857600  # 100MB in bytes

# Roboflow License Plate Detection (Hosted API)
# Your hosted endpoint pattern:
# https://serverless.roboflow.com/<project>/<version>?api_key=...
ROBOFLOW_API_KEY=your_roboflow_api_key
# Optional single-var override (recommended): "<project>/<version>"
# Example: ROBOFLOW_MODEL=plate-detector-i9vkk/5
ROBOFLOW_MODEL=
ROBOFLOW_PROJECT=plate-detector-i9vkk
ROBOFLOW_VERSION=5
ROBOFLOW_ENDPOINT_BASE=https://serverless.roboflow.com
ROBOFLOW_TIMEOUT_S=60
# Optional tuning (Roboflow supports these query params; values are % 0-100, or 0-1.0 fraction)
# Lower confidence detects more plates but can increase false positives.
# 0 is the absolute lowest (most permissive).
ROBOFLOW_CONFIDENCE=0
ROBOFLOW_OVERLAP=30

# License plate blurring behavior
PLATE_BLUR_ENABLED=1
# Expand detected box (set >0 only if you want extra margin). 0 = exact box.
PLATE_BLUR_EXPAND=0
# If 1, also saves an "original_*" copy next to the blurred image
PLATE_BLUR_KEEP_ORIGINAL=0