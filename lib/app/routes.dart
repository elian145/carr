import 'package:flutter/material.dart';

import 'production_routes.dart';

/// Test / migration shell routes — same table as production.
Map<String, WidgetBuilder> buildAppRoutes() => buildProductionRoutes();
