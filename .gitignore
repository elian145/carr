node_modules/
npm-debug.log*
dist/
.env
outputs/*.tmp

# Ignore local uploads and temp outputs
kk/static/uploads/
kk/instance/*.db
static/uploads/
instance/
kk/instance/
**/*.db
**/*.db-journal
**/*.sqlite
**/*.sqlite3
**/*.log

# Model weights and large binaries
*.pt
kk/**/*.zip
kk/**/*.7z
kk/**/*.rar
kk/**/*.tar
kk/**/*.tar.gz

# Flutter/Build outputs
.flutter-plugins-dependencies
build/
**/DerivedData/
**/.dart_tool/
**/.fleet/
**/.idea/
**/.vscode/

# OS junk
.DS_Store
Thumbs.db

# Python cache
__pycache__/
*.pyc

# Backend local virtualenv and secrets
backend/.venv/
backend/env.local
backend/blurred_*.jpg
backend/blurred_one.jpg
backend/*.bin

# Repo-root Python venvs and local tools
.venv/
.venv_plate/
kk/env.local
kk/instance/*.bak*
kk/backend_run.log
cloudflared.exe
python310.exe
releases_windows.json
android/.kotlin/
android/.gradle/

# Android SDK/plugin generated assets
android/app/src/main/assets/airbridge.json
android/app/src/main/assets/libraryInfo.json

# Android signing and local machine paths (never commit)
signing.properties
android/signing.properties
android/local.properties

# Keystores (never commit)
*.jks
*.keystore

# Scratch outputs (never commit)
tools/scratch/
tools/out/

# Local scratch scripts (never commit)
kk/_*.py
kk/check_*.py

# Firebase service account exports (never commit)
firebase-service-account*.txt
firebase-service-account*.json

# Accidental CLI artifact files (repo root)
Get
Run
Process
tatus
tash list
e -Name*
analyze.txt
*_files.txt
current_files.txt
recovered_files.txt
restored_*_files.txt
main_files.txt
test_connectivity.jpg
visual_test.jpg
